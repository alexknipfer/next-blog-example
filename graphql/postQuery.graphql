query Post($where: PostWhereUniqueInput!) {
  post(where: $where) {
    title
    id
    slug
    coverImage {
      url
    }
    author {
      title
      name
      picture {
        url
      }
    }
    content {
      html
    }
  }
}
